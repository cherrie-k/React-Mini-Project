// setBodyColor와 동일한 역할을 한다.

// 한 번 About page에 다녀오면 다른 pages들의 background들도
// color가 아닌 image로 바껴있는 버그가 생겨 일단 유배 시켜둠.

/*
export default function setBodyImage({ image }) {
  document.documentElement.style.setProperty("--bodyImage", image);
}
*/
